#include <iostream>
using namespace std;
#include <iostream>
#include <fstream>
#include <ctime>
#include <string>
using namespace std;

//#include "debugLogger.h"
/**
 * this function is to show an example for the functions of the Debuglogger
 */

//void funktionToShowTheLogger() {

	/**
	 * shows the time when you entering and exit the function
	 */
//	LOG_SCOPE
//	;
	/**
	 * setting output level to 2, you can use LOG_DEBUG, LOG_WARNING and LOG_ERROR messages
	 */
//	LOG_SET_LEVEL(DEBUG);
	/**
	 * prints a debug message with the system time in the logfile
	 */
//	LOG_DEBUG << "prints this debug message with the system time in the logfile \n";
	/**
	 * setting output level to 1 you only can use LOG_WARNING and LOG_ERROR messages
	 */
//	LOG_SET_LEVEL(WARNING);
	/**
	 * prints a warning message with the system time in the logfile
	 */
//	LOG_WARNING << "prints this warning message with the system time in the logfile \n";
	/**
	 * setting output level to 0 you only can use LOG_ERROR messages
	 */
//	LOG_SET_LEVEL(ERROR);
	/**
	 * prints a error message with the system time in the logfile
	 */
//	LOG_ERROR << "prints this error message with the system time in the logfile \n";
//}

int maintestlogger() {
//	funktionToShowTheLogger();
	cout << "Logging example is ready!" << endl;
        return 0;
};

